import 'package:flutter/material.dart';

class BubbleShort extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.redAccent,
      ),
    );
  }
}
